# Vox Frontend

Next.js app for landing page, upload workflow, processing state, transcript editor, subtitle editor, summary panel, and export downloads.

## Run

```bash
npm install
../scripts/dev-frontend.sh
```

Set `NEXT_PUBLIC_API_URL=http://127.0.0.1:8010`.
